export default class UserModel {
    constructor (id, name, email, roleName, createdAt) {
        this.id = id
        this.name = name
        this.email = email
        this.roleName = roleName
        this.createdAt = createdAt
    }
}